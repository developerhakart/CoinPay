# Vault Sensitive Files
# DO NOT commit these files to git!

# Unseal keys (CRITICAL - needed to unseal Vault)
unseal-keys.json
unseal-keys.*.json

# Root token (CRITICAL - full access to Vault)
.root-token
root-token
*.token

# Vault data directory (contains encrypted secrets)
data/
*.db

# Vault audit logs (may contain sensitive information)
*.log
audit.log

# Backup files
*.bak
*.backup
