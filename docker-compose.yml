version: '3.8'

services:
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: coinpay-postgres
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: root
  #     POSTGRES_DB: coinpay
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - coinpay-network
  #   restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: CoinPay.Api/Dockerfile
    container_name: coinpay-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=coinpay;Username=postgres;Password=root
      - Circle__ApiKey=TEST_API_KEY:d93edad9d7011eae471468f01252bafa:8cc4aae56a478a0a313914a062be0445
      - Circle__EntitySecret=dc1ff0c795a9701035d45927a8cfc3dd54255f19e1ceebb8e50bafeaf2493d26
      - Circle__WebhookSecret=test_webhook_secret_def456ghi012jkl345mno678pqr901stu234
      - Circle__ApiBaseUrl=https://api.circle.com/v1/w3s
      - Jwt__SecretKey=DevelopmentSecretKey_ChangeInProduction_MinimumLength32Characters
      - Jwt__Issuer=CoinPay
      - Jwt__Audience=CoinPay
      - Jwt__ExpirationMinutes=1440
      # Blockchain Test Wallet Configuration (for real POL/USDC transfers)
      # IMPORTANT: Replace YOUR_PRIVATE_KEY_HERE with actual private key from MetaMask
      # Get free testnet POL: https://faucet.polygon.technology/
      - TEST_WALLET_PRIVATE_KEY=YOUR_TEST_WALLET_PRIVATE_KEY_HERE
    ports:
      - "7777:8080"
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    networks:
      - coinpay-network
    restart: unless-stopped

  gateway:
    build:
      context: .
      dockerfile: CoinPay.Gateway/Dockerfile
    container_name: coinpay-gateway
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
    ports:
      - "5000:8080"
    depends_on:
      - api
    networks:
      - coinpay-network
    restart: unless-stopped

  web:
    build:
      context: ./CoinPay.Web
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://localhost:5000
    container_name: coinpay-web
    ports:
      - "3000:80"
    depends_on:
      - gateway
    networks:
      - coinpay-network
    restart: unless-stopped

  docfx:
    build:
      context: .
      dockerfile: docfx/Dockerfile
    container_name: coinpay-docs
    ports:
      - "8080:80"
    networks:
      - coinpay-network
    restart: unless-stopped

  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   container_name: coinpay-pgadmin
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@coinpay.com
  #     PGADMIN_DEFAULT_PASSWORD: admin
  #   ports:
  #     - "5050:80"
  #   depends_on:
  #     - postgres
  #   networks:
  #     - coinpay-network
  #   restart: unless-stopped

volumes:
  postgres-data:

networks:
  coinpay-network:
    driver: bridge
